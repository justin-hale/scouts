name = "scouts-calendar"
main = "worker.js"
compatibility_date = "2025-01-01"

# Public (non-secret) configuration. Edit these to match your setup.
[vars]
GIST_ID = "44cd99dc175d3f87abfffa47aea6f850"
GIST_FILENAME = "calendar.json"
# Comma-separated allowlist of origins permitted to call this Worker.
ALLOWED_ORIGIN = "https://bolivarscouts.com,https://www.bolivarscouts.com"

# Secrets are NOT set here. Set them with wrangler so they stay encrypted:
#   npx wrangler secret put GITHUB_TOKEN
#   npx wrangler secret put ADMIN_PASSWORD
